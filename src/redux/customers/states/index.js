const initialState = {
    data: [],
    activeCustomerId: null,
    isVisible: {
        addForm: false,
        changeForm: false,
        deleteForm: false,
    },
};

export default initialState;
